@page "/personas"
@using Actividad18.Data
@inject IPersonaRepository PersonaRepository

<h3>Gestión de Personas</h3>

<button @onclick="NuevoRegistro">Agregar Nueva Persona</button>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Edad</th>
            <th>Dirección</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var persona in personas)
        {
            <tr>
                <td>@persona.Id</td>
                <td>@persona.Nombre</td>
                <td>@persona.Edad</td>
                <td>@persona.Direccion</td>
                <td>
                    <button @onclick="() => EditarPersona(persona.Id)">Editar</button>
                    <button @onclick="() => EliminarPersona(persona.Id)">Eliminar</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@code {
    private List<Persona> personas = new();

    protected override async Task OnInitializedAsync()
    {
        personas = await PersonaRepository.GetAllAsync();
    }

    private async Task EliminarPersona(int id)
    {
        await PersonaRepository.DeleteAsync(id);
        personas = await PersonaRepository.GetAllAsync();
    }

    private void NuevoRegistro()
    {
        // Lógica para agregar una nueva persona
    }

    private void EditarPersona(int id)
    {
        // Lógica para editar una persona existente
    }
}
